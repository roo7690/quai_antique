
CREATE table galerie (
title VARCHAR(250),
file VARCHAR(250) NOT NULL
);

CREATE table produit (
title VARCHAR(250),
description TEXT,
TYPE VARCHAR(20),
prix FLOAT
);

CREATE table menu (
title VARCHAR(250),
description TEXT,
prix FLOAT
);

